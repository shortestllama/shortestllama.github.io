title: Jack Ford

collections:
  writeups:
    output: true
  personal:
    output: true

defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
  - scope:
      path: ""
      type: "personal"
    values:
      layout: "personal"
  - scope:
      path: ""
      type: "writeups"
    values:
      layout: "writeups"
  - scope:
      path: ""
    values:
      layout: "default"

plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag
